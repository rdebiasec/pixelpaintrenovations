{
  "name": "pixelpaintrenovations-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/i18n-build.mjs && node scripts/generate-seo.mjs && vite",
    "build": "npm run build:static",
    "build:static": "node scripts/i18n-build.mjs && node scripts/generate-seo.mjs && vite build && node scripts/check-dist.mjs",
    "build:gh-pages": "npm run build:static",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "vite": "^8.1.5"
  }
}
