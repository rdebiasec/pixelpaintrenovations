{
  "name": "pixelpaintrenovations-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:static",
    "build:static": "node scripts/generate-seo.mjs && node scripts/i18n-build.mjs && vite build && node scripts/check-dist.mjs",
    "build:gh-pages": "npm run build:static",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "vite": "^8.1.4"
  }
}
